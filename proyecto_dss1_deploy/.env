
# Copy this file to .env and edit as needed

# Database
DB_NAME=monitoreo_db
DB_PASSWORD=example
DB_PORT=3306

# Frontend -> Backend base URL (what the browser should call)
# In local dev with compose, leave it pointing to localhost:8000
NEXT_PUBLIC_API_BASE=http://localhost:8000
